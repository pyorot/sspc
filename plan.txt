Outline
(active reload) on left press, if reload trigger neutral:
  1. asm cf.fileLoadSelected()               (copies FS → FA)
  2. asm loadToCurrent()                     (copies FC → current)
  3. load spawn if location not null
  4. trigger reload ('5')
(BiT reload) on up press, if reload trigger neutral:
  1. asm cf.reset()                          (blanks FA+FB; runs loadToCurrent())
  2. set spawn to (Skyloft, 0x1c, 0x3f)
  3. trigger reload ('5')
(save) on right press:
  1. save spawn
  2. asm cf.fileSaveSelected()               (copies FA → FS)
(clear) on Z+right press:
  1. clear spawn location

patch cutscenes